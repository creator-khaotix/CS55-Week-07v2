runConfig:
  # Your run configuration settings, e.g.,
  minInstances: 0
  maxInstances: 2
env:
  
  # This is the critical part for your Gemini API Key
  - variable: GEMINI_API_KEY
    secret: GEMINI_API_KEY # This should match the name you gave your secret when setting it
